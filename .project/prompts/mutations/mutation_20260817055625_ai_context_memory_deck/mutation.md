# Mutation Log

## Created
- **docs/demos/demo-20260817-ai-context-memory.md** — Single self-contained, 22-slide markdown deck focused specifically on HypergraphAI as an AI-agent memory engine across five memory tiers: working, session, episodic, semantic, and procedural. Defines each tier, maps it to a concrete hgai data pattern (tags, hypernode/hyperedge shape, PIT), and walks all five through one running example (an SRE-assistant agent investigating a production incident for a fictional customer, "Acme Corp"), centered on a 4-member `incident-conclusion` hyperedge (service + originating session + evidence + conclusion) as the deck's centerpiece N-ary example. Includes: four single-tier HQL retrieval queries and one verified-safe multi-hop cross-tier SHQL query (every variable pre-bound via top-level `node:` patterns to avoid the engine's member-sub-pattern type-checking ambiguity); an MCP write/read tool-call walkthrough; three dedicated "why hgai over X" sections specific to the memory use case (SQL/RDBMS schema-rigidity-for-emergent-memory-shapes, RDF/triple-store reification-cost-plus-poor-fit-for-high-churn-ephemeral-tiers, and vector-only-store tier-blindness/no-exact-lookup/no-PIT), a comparison table, a tier lifecycle/retention-hygiene section, a live-vs-roadmap honesty section, an architecture diagram, and a getting-started/key-takeaways close.
