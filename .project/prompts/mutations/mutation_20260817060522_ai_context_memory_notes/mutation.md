# Mutation Log

## Created
- **docs/demos/demo-20260817-ai-context-memory-notes.md** — Speaker notes companion to `docs/demos/demo-20260817-ai-context-memory.md`. Placed under `docs/demos/` (plural) to match the deck's actual location — the user's message referenced `docs/demo/` (singular) for both files, treated as a typo since the deck itself was created at `docs/demos/` in the prior turn. Contains two emphasized deep-dive sections requested by the user: "Why hypergraphs specifically fit agentic AI" (the N-ary shape-match between an agent's own unit of work and a hyperedge, MCP/protocol-native structured-object alignment, precision-over-recall retrieval under context scarcity, point-in-time semantics for concurrent/async agents, and symmetric agent-writable/human-writable knowledge) and "How agentic AI should model knowledge to fit" (five ordered modeling heuristics: find the unit of reasoning, count real participants honestly, give reusable facts their own typed node instead of a string attribute, timestamp instead of overwrite, attach provenance/confidence to the fact itself). Also includes suggested timing, delivery notes on the deck's three load-bearing sections, and an anticipated-questions section tailored to a technical/mixed audience (comparisons to LangChain/LlamaIndex memory modules and dedicated products like Zep/mem0, comparison to property-graph databases like Neo4j specifically as distinct from the deck's RDF/triple-store comparison, an honest "is this over-engineering for a small agent" answer, and an honest note that tag-based tier discipline is an application-level responsibility today, not automatic).
