# Mutation Log

## Created
- **docs/design/epics-to-build-production-hgai/hgai-v0.2-plan-prompts.md** — A set of 13 ready-to-submit prompts (Prompt 0 through Prompt 12), one per phase of `build-plan.md` §14, plus a one-time setup note. Each prompt is self-contained (re-states its phase's scope, references the prior-phase repo state rather than chat history) so it can be submitted to Claude Code or a similar agentic coding assistant as an independent session.

## Modified (rename)
- **docs/design/epcis-to-build-production-hgai/** → **docs/design/epics-to-build-production-hgai/** — Renamed the directory (containing `build-plan.md`) to fix a typo ("epcis" → "epics"), matching the path the user specified in this request. No content changes to `build-plan.md` itself.
