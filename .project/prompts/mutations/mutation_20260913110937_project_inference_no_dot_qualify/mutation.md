# Mutation Log

## Modified
- **hgai/core/inference.py** — `project_inference()`'s `only_new_hyperedges=True` path no longer rewrites member ids. Removed the `_resolve_member_ref` helper and the `resolved_members`/`resolved_member_refs` machinery introduced in the immediately preceding turn entirely; the per-candidate loop is back to using `_member_ids(candidate)`/`candidate["members"]` directly (identical to the `only_new_hyperedges=False` path), with the sole remaining difference being that the `_ensure_member_exists` duplication loop is skipped when `only_new_hyperedges` is set. Rewrote the docstring section explaining `only_new_hyperedges` to describe the corrected behavior and why a bare reference is sufficient (verified: `HypernodeSearchFilters`/`HyperedgeSearchFilters.hypergraph_ids` is a list, queried with Mongo `$in`, so a multi-graph or logical/composed query resolves a member id across every graph in scope, not just the one the referencing edge lives in).
- **hgai/api/routers/inference.py** — Corrected `ProjectInferenceRequest.only_new_hyperedges`'s field description to match: members are left as-is, not graph-qualified.
- **ui/index.html** — Corrected the "Project only new hyperedges?" toggle's inline explanatory text to describe the corrected behavior (references left exactly as in the source; resolve correctly once the target is queried alongside its source(s)), removing the now-inaccurate mention of a `source-graph-id.member-id` qualified form.
- **ui/js/app.js** — No changes this turn (the `piBuildRequest()`/preview-rendering code already just passes through and displays whatever `only_new_hyperedges`/`members` the backend sends — nothing there assumed graph-qualification, so nothing needed correcting).
