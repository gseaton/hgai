# Prompt

When generating projected hyperedges, please do NOT modify (e.g. adding the '<hypergraph-id>.' to the beginning of relation and member references) the hyperedge references (relation, members, etc).  For example, if a member reference in the source hypergraph is 'person:shemp', do NOT add a source hypergraph dot qualifier like 'hello-world.person:shemp'.
