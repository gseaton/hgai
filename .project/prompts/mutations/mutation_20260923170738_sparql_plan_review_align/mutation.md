# Mutation Log

## Modified
- **docs/architecture/sparql-to-shql-conversion-plan.md** — Rewrote to fully align with `sparql-vs-shql-gaps.md`'s tiered findings, fixing six specific inconsistencies found during review:
  1. §2's `BIND` row claimed output-only support with no phase implementing it — added output-only `BIND` handling to Phase 4 (client-side evaluation over fetched rows), matching its Tier 1 status.
  2. Phase 1 excluded `ASK` from v1 scope, contradicting Tier 2's "build — nearly free" verdict — moved `ASK` into Phase 1's in-scope query forms and added its translate-to-`SELECT`+`limit:1` handling to Phase 4.
  3. Tier 2's `^p` (inverse) and fixed-length `p1/p2` property-path special cases were never added to Phase 2.5 despite gaps.md's explicit phase-mapping — added both, with the fixed-length case marked a stretch goal per its lower Tier 2 priority.
  4. Phase 3 blanket-rejected `FILTER EXISTS`/`NOT EXISTS` — replaced with the Tier 3-recommended `OPTIONAL`+`BOUND()` approximation for simple cases, rejecting only more complex forms.
  5. Phase 5 used "quietly drops a `MINUS` clause" as its illustrative bad example while Phase 3 now approximates `MINUS` — replaced the example with `CONSTRUCT` (a genuinely rejected construct) and added the Tier 3 two-query-subtraction approximation for `MINUS` to Phase 3, with its documented-divergence caveat.
  6. Phase 5's rejection layer was generic — added the specific per-construct reasoning from Tier 4/5 (mesh dot-notation instead of `SERVICE`, native query/export instead of `CONSTRUCT`/`DESCRIBE`, the REST/CRUD/MCP API instead of SPARQL Update, RDF-import data loss instead of "not implemented" for `lang()`/`datatype()`/`str()`).
  Also updated: the lead paragraph's supported-construct list, §2's `MINUS`/property-path/`ASK` rows, §3's SHQL-cons paragraph, §4's closing note distinguishing the Tier 5 import-time gap from query-time gaps, Phase 6's validation cases (added `ASK`, output-only `BIND`, `^p`, `FILTER EXISTS`, and `MINUS` approximation cases, with the latter two asserting documented divergence rather than exact parity), and §6's recommendation paragraph (now precisely maps each tier onto its scope decision instead of an imprecise "some things need an engine change" grouping).
- **docs/architecture/sparql-vs-shql-gaps.md** — Updated the cross-reference note to reflect that Tier 1 *and* Tier 2 *and* Tier 3 (not just Tier 1's `VALUES`/aggregates) are now folded into the plan, and that Tier 4/5's reasoning is threaded into the plan's Phase 5 rejection layer rather than only referenced.
