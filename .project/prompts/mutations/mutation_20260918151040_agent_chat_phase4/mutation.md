# Mutation Log

## Modified
- **ui/js/api.js** — Added the `HGAI_API` methods for the AI Agent feature: `listAgentVendors`/`getAgentVendor`/`createAgentVendor`/`updateAgentVendor`/`deleteAgentVendor`, the same set for models, and `listAgentSessions`/`getAgentSession`/`createAgentSession`/`updateAgentSession`/`deleteAgentSession`/`listAgentMessages`/`sendAgentMessage`/`saveAgentMessageAsNote`. Added `streamAgentMessage()` — a manual `fetch` + `ReadableStream` reader that parses `\n\n`-delimited SSE frames (the generic `request()` helper always awaits `resp.json()`, so streaming needed its own path, same reasoning as the existing `uploadMedia`/`downloadMedia` manual-fetch functions).
- **ui/index.html** — Added the "AI Agent" sidebar link (admin-only) and its screen (`screen-agent-admin`: a Vendors table+modal and a Models table+modal, following the Parameterized Queries/Meshes table+card conventions exactly, no pagination since these are small admin-managed lists like Meshes). Added a chat-toggle button to the topbar. Added `#agent-chat-panel` as a direct sibling of `#main-content` inside `#app-shell` (not scoped to any one screen) — header (model picker, sessions/new-session/close buttons), a collapsible session list, an empty state, a scrollable message list, and a composer. Added the Vendor and Model create/edit modals.
- **ui/css/hgai.css** — Added the AI Agent Chat Panel section: panel layout (fixed-width flex column, `.d-none` toggle — the same show/hide idiom every other screen/column in this app already uses, not a new width-animated collapse), session-list items, message bubbles (user/assistant, using existing `--hgai-*` theme tokens so all four themes work automatically), and a mobile (`max-width: 991.98px`) full-screen override.
- **ui/js/app.js** — Added `State` fields for vendor/model/session caches and chat UI state; router entries for `agent-admin`; full Vendor/Model CRUD screen logic (`loadAgentAdmin`, `loadAgentVendors`/`openAgentVendorModal`/save/delete, same for models, mirroring `loadPQ`/`openPQModal` line for line); and the chat panel logic (`initAgentChatPanel`, `loadAgentChatModelOptions`, `loadAgentChatSessions`, `selectAgentChatSession`, `appendAgentChatMessageEl`, `sendAgentChatMessage` — non-streaming user bubble appended immediately, then `HGAI_API.streamAgentMessage` fills the assistant bubble incrementally via `onDelta`, wires the Save-as-Note button via `onDone`'s `message_id`, and renders `onError` inline — and `saveAgentChatMessageAsNote`).
- **hgai/core/notes.py**, **hgai/main.py**, **hgai/config.py**, **pyproject.toml**, **requirements.txt**, **hgai_module_agentchat/**, **tests/test_agent_chat.py** — Unchanged in this phase; listed by `git status` only because they were already modified in Phases 1–3 (see their own mutation records) and remain uncommitted.

No files were created or deleted in this phase (all changes are edits to existing files from Phase 1–3's scaffolding).
