# Mutation Log

## Modified
- **ui/index.html** — Added a "Show inferred edges" checkbox toggle (`id="viz-show-inferred"`) to the Visualize screen's filter bar, right after the existing "Hide orphan nodes" toggle, with a title tooltip explaining it renders facts derived via inverse-of/symmetric/superproperty axioms, computed live and never persisted.
- **ui/js/app.js** — Added `VIZ_INFERRED_COLOR = '#c084fc'` constant. Added `fetchInferredEdges(graphId)`, which runs an `infer: true` HQL query scoped to `type: hyperedge` (axiom-expansion only, not transitive-closure) and returns items tagged `_inferred: true` with a synthetic id. Wired it into `renderViz()`'s Phase 1 fetch loop (opt-in via the new checkbox, concatenated onto the graph's literal `edges` array before focus-filtering). Propagated an `_inferred` flag and `VIZ_INFERRED_COLOR` through Pass A's henode creation, Pass B's members-node/"hyperedge"-link/relation-links creation, the `.nodeLabel()` tooltip builder (adds an "⚡ inferred — computed live, not persisted" note), the `onNodeClick`/`onLinkClick` detail-panel builders (add `_inferred`/`_source_edge`/`_axiom` fields), and `buildVizLegend()` (new `anyInferred` parameter, new "⚡ inferred" legend row).
