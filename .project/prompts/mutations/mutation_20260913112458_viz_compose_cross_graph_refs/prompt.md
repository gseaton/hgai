# Prompt

Please support composing of hypergraphs for Visualize. For example, if some of the selected hypergraphs have the canonical hyperedges/hypernodes, and a generated hyperedges-only hypergraph is included, the links from the generated inferred hyperedges in the hyperedges-only hypergraph should be shown linking to the canonical hyperedges/hypernodes given the reference is correct (e.g. 'person:shemp' reference in the hyperedges-only hyperedges hypergraph should link to the 'person:shemp' from the included composed explicit canonical hypernode 'person:shemp').  In addition, also support links from one hypergraph reference (e.g. hypernode 'person:shemp' is referenced in included hypergraph B but the hypernode 'person:shemp' is defined in include hypergraph A, the Visualize should honor / make Visualize links properly the references in hypergraph B hyperedge(s)).
