# Prompt

Please add a 'Suppress Attribute Prefixes' toggle to strip the prefixes (if enabled) of attribute names (e.g. 'ex:sex' -> "sex", 'http://example.org/description" -> "description") for the import Hypergraph window.
