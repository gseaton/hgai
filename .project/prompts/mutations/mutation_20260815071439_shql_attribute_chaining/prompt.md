# Prompt

**Turn 1**

Is it necessary to provide shots / examples in the' 6. Query Layer' section of the build-plan.md?  Or is it better to have Claude decide during implementation the syntax?  There is no reference to SPARQL-esqe semantic flavor for SHQL pattern matching and no chaining out to matched hypernode and hyperedge attributes from more than the initial query matches.  Should there be something in the query layer definition to allow for at least access to the attributes of matched query nodes?

---

**Turn 2**

Yes, apply that edit to §6.2
