# Mutation Log

## Modified
- **hgai/core/rdf_import.py** — Added a second axiom hyperedge emitted for every `<P> a owl:SymmetricProperty` triple: a `hub`-flavored edge with `relation="owl:symmetric"` and a single member `<P>` (seq 0), alongside the pre-existing `symmetric`-flavored data edge for each triple using `<P>`. Extended the internal `edge_tuples` shape from `(relation, member_ids, sort_key)` to `(relation, flavor, member_ids, sort_key)` to let this new axiom edge share the same emission path with the existing `owl:transitive` axiom edge.
- **tests/test_rdf_import.py** — Renamed/rewrote `test_owl_symmetric_property_data_edge_gets_symmetric_flavor` to `test_owl_symmetric_property_reproduces_the_request_exactly_two_edges` (now asserts both edges from the user's exact example); renamed/rewrote `test_owl_symmetric_property_produces_no_separate_axiom_edge` to `test_owl_symmetric_property_also_produces_a_single_member_axiom_edge` (now asserts the `owl:symmetric` axiom edge exists); updated `test_every_triple_using_a_symmetric_property_gets_the_flavor_not_just_the_first` to account for the added axiom edge (3 data edges + 1 axiom edge = 4 total); added an `owl:symmetric` axiom-edge assertion to `test_symmetric_transitive_and_inverse_of_can_coexist_on_different_properties`.
- **docs/help/notes/web-ui/export-import.md** — Updated the RDF mapping table's `owl:SymmetricProperty` row to describe both the `symmetric`-flavored data edge and the new `owl:symmetric` axiom edge (recognized by `infer: true`); removed `owl:SymmetricProperty` from the "not specially recognized" list in the "Not handled" paragraph.
- **docs/api-reference.md** — Same update to the equivalent RDF mapping table row and "Not handled" paragraph.
