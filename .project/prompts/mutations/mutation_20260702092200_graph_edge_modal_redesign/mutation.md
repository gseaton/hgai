# Mutation Log

## Modified
- **ui/index.html** — Replaced `#modal-graph` with a redesigned layout: styled header (indigo gradient, icon badge, subtitle), body sectioned into Identity / Classification / Attributes with `node-form-section` dividers, dark JSON editor, and styled footer with floppy icon on Save. Changed `graph-description` from `<textarea rows="2">` to `<input type="text">` to match Hypernode pattern. Changed `graph-id` input to add `font-monospace` class.
- **ui/index.html** — Replaced `#modal-edge` with a redesigned layout: styled header (cyan gradient, icon badge, subtitle), body sectioned into Identity / Classification / Temporal Validity / Members / Attributes, full-width Hypergraph select/display rows, dark JSON editor, and styled footer. Changed `edge-graph-select-row` and `edge-graph-display-row` from `col-md-6` to `col-12`. Added `node-graph-display-field` class to `#edge-graph-display`. Added `font-monospace` to `#edge-id` input. Changed `btn-add-member` color from `btn-outline-primary` to `btn-outline-info` to match the edge entity color.
- **ui/css/hgai.css** — Consolidated `.node-modal-footer`, `.graph-modal-footer`, `.edge-modal-footer` into a single shared rule. Added `.graph-modal-header`, `.modal-entity-badge-graph`, `#modal-graph .modal-body`, `#modal-graph .form-label`. Added `.edge-modal-header`, `.modal-entity-badge-edge`, `#modal-edge .modal-body`, `#modal-edge .form-label`.
