# Prompt

Please add 'mutations' list field of hypernodes/hyperedges that tracks mutations (create, mutate) ('ts', 'by', 'mutation' ('create', 'mutate', ?), 'delta' (list of deltas). Please ensure proper logic and UI for mutations and do NOT make redundant sequential mutations (if candidate mutation item is the same as the most current mutation item, they do not persist).
