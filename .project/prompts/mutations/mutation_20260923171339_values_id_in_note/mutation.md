# Mutation Log

## Modified
- **docs/architecture/sparql-to-shql-conversion-plan.md** — Added a note after Phase 2.5's bullet list explaining: (1) that single-field in-list matching already exists natively in SHQL today via two mechanisms (`filter: "?var.field IN [...]"` and `attributes: {field: {$in: [...]}}`), verified directly against `hgai_module_shql/engine.py`; (2) why neither covers SPARQL `VALUES` (no multi-variable tuple binding, no way to inject a binding without a backing document search), which is why the plan unrolls `VALUES` into a `union:` instead; and (3) a flagged-but-out-of-scope possible future SHQL engine enhancement — letting the node pattern's `id:` key accept a list, since the storage filter it already builds (`HypernodeSearchFilters.node_ids_in`) supports multi-id matching but the YAML surface never exposes it — which would let the transpiler translate the common "`VALUES` enumerates candidate ids for one node pattern" case more cheaply than a full `union:` unroll, without being part of this plan.
- **docs/architecture/sparql-vs-shql-gaps.md** — Added the same note, immediately after the Tier 1 table (where `VALUES`'s cost/verdict is scored), worded for this document's own framing: ties the "why `union:` unrolling, not something simpler" explanation directly to Tier 1's `VALUES` row, and frames the `id:`-list idea explicitly as outside this tier's "build now" recommendation — a possible follow-on worth a separate look only if real-world `VALUES` usage turns out to be dominated by the single-variable-candidate-list shape.
- **docs/help/notes/web-ui/rdf-import.md** — Added a new "Coming from SPARQL?" section (after Caveats, before See also) with the same underlying facts adapted for this topic's end-user audience: leads with the practical, already-shipped part (SHQL's `filter: "... IN [...]"` and `attributes: {field: {$in: [...]}}` already work today against RDF-imported attributes/ids), explains briefly why neither is full SPARQL `VALUES`, and points to the two `docs/architecture/` documents (referenced as plain code-formatted repo paths, consistent with how this topic already references `hgai/core/rdf_import.py`, since Help's `help:`-id link syntax has no way to address a non-Help-topic file) for the forward-looking conversion plan.
- **docs/help/notes/shql/shql-filters.md** — Extended the existing "Filter or attribute operator?" section with one sentence pairing `IN` (filter-side) with `attributes: {field: {$in: [...]}}` (pattern-side, already documented in `shql-patterns.md`'s "MongoDB operators" section, cross-linked rather than re-explained) as the same idea at two different evaluation points. Added a new "Coming from SPARQL?" section stating both already cover SPARQL `VALUES`'s common case, explaining briefly what full `VALUES` does that neither can, and pointing to the two `docs/architecture/` documents for the fuller conversion effort.
- **docs/help/notes/shql/shql-patterns.md** — Added a new "`id:` takes one value, not a list" section, placed right where this document already covers the `id:`/`node_id:` fields: clarifies that `$in` (already documented here for `attributes:`) doesn't apply to `id:`/`node_id:`, names the two existing workarounds (a `union:` branch per id, or binding-then-filtering with `IN`), and notes the underlying storage search already supports multi-id matching even though the YAML surface doesn't expose it. Added a "Coming from SPARQL?" section tying that `union:`-per-id technique directly to what a `VALUES` clause enumerating ids would translate to, and pointing to the two `docs/architecture/` documents for the fuller conversion effort.
