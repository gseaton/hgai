# Mutation Log

## Modified
- **docs/design/epics-to-build-production-hgai/build-plan.md** — Added a new **Phase 13 — 3D interactive hypergraph visualization (optional)** entry in §14 (Build Phases), inserted after Phase 12 and before §15. Marked explicitly optional (start only after Phase 12's exit criteria pass; the platform is complete and usable without it). Specifies: a hypergraph-to-binary-scene-graph rendering mapping (hypernode → scene node, hyperedge → its own scene node with one link per member, seq-ordered, directional per flavor); a recommended library (`3d-force-graph`, Three.js-based, self-hostable); an initial scene-load strategy reusing the existing `GET /graphs/{id}/nodes`/`edges` list endpoints with a size-bound guardrail; manual rotation via default `OrbitControls`; an auto-rotate toggle with idle-timeout resume behavior; server-side filtering reusing the existing list-endpoint query params (§7); and a double-click focus interaction that reuses the already-specified HQL member sub-field filtering (§6.1) and REST edge hydration (`?hydrate=1`, §7) to fetch and merge a focused node's one-hop neighborhood, animate the camera, and highlight/dim accordingly. No backend/API changes required — the phase is additive to `apps/web-ui` only. Exit criteria specified as a manual smoke test (consistent with Phase 11's UI-phase verification standard) plus component tests for the filter-fetch logic and the scene-graph transform function.
