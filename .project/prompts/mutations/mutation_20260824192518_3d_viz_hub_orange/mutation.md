# Mutation Log

## Modified
- **ui/js/app.js** — In the `VIZ_FLAVOR_LINK_COLOR` map (added in `mutation_20260824192127_3d_viz_flavor_colors`), changed the `hub` entry from gold (`#d4af37`) to orange (`#f97316`). No other flavor colors, structures, or logic changed.
