# Prompt

Please fix the Visualize inferred edges rendering issue so that when the 'Show inferred edges' is enabled/disabled, that the Visualize re-renders accordingly.  Currently, the Visualize does NOT re-render when 'Show inferred edges' is toggled, but renders correctly when another event (like 'Hide orphan node' is toggled) is fired.
