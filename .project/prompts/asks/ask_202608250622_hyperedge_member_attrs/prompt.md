# Prompt

Please assess how member nodes are stored in hyperedges.  If the member node identifiers are only used (i.e. hypernode members fields is just an array of string identifiers), then plan how to refactor the hypernode members to be an array of dict/maps with the 'id' key being the member node identifier and the rest of the member document / map attributes of that member in the context of that hyperedge.
