# Prompt

Please review and assess if the --Select Graph-- dropdown at the top right corner of the UI is used for anything at this point. If so, what uses the --Select Graph-- dropdown.  If nothing is currently using the --Select Graph-- dropdown, please remove the control dropdown if safe to do so.
