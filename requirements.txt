# HypergraphAI dependencies

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.32.1
starlette==1.6.0

# MCP (Model Context Protocol)
mcp==1.30.0

# MongoDB async driver
motor==3.7.0
pymongo>=4.10,<5.0

# Data validation
pydantic==2.13.4
pydantic-settings==2.15.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=3.2.0,<4.0.0
python-multipart==0.0.18

# Environment
python-dotenv==1.0.1

# YAML
pyyaml==6.0.2

# RDF import (Turtle, RDF/XML, JSON-LD, Notation3) — hgai/core/rdf_import.py
rdflib==7.6.0

# HTTP client (for mesh/remote graph queries)
httpx==0.28.1

# S3-compatible object storage (optional media backend, HGAI_MEDIA_BACKEND=s3)
boto3==1.43.79

# Audio/video duration extraction for uploaded media (best-effort)
mutagen==1.48.1

# Utilities
python-dateutil==2.9.0

# Shell
prompt_toolkit==3.0.48

# AI Agent chat (vendor-agnostic agent framework + model SDKs).
# Deliberately NOT agno[mcp] — that extra pins mcp>=2.1.0, which conflicts
# with this project's own mcp==1.30.0 (see hgai_module_mcp). The agent chat
# module talks to the resident MCP server via a small custom httpx-based
# client instead (hgai_module_agentchat/mcp_toolkit.py).
agno==3.0.10
anthropic==1.7.0
openai==3.16.0
cryptography>=3.4.0

# Web access tool for the chat agent (hgai_module_agentchat/web_toolkit.py) —
# HTML parsing for a single-page fetch/read tool. Deliberately a small
# custom toolkit over agno.tools.website.WebsiteTools, whose read_url()
# defaults to crawling up to 3 levels / 10 links per call.
beautifulsoup4==4.15.0
